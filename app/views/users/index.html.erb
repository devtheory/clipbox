<br />
<br />
<div class="container">
  <% @users.each do |user| %>
    <%= user.name %>
    <%= image_tag(user.image) %>
  <% end %>
  <%= link_to "Send email", user_send_path(current_user), class: 'btn btn-primary' %>
</div>